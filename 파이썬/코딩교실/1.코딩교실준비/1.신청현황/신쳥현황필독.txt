1. 이 매크로의 경로에는 한글이 들어가있으면 오류가 납니다.
오류를 방지하기 위해선 sincheong 폴더를 C 폴더에 이동해서 py파일을 실행해야 합니다. (필요에 따라 바로가기를 만들어 경로를 이동합니다.)

2. 엑셀에서 화면캡쳐를 사용하고 마우스를 직접 이동시키기 때문에 동작 중엔 마우스를 건들이면 안 됩니다.

3. 신청현황확인.py를 실행시키면 코딩교실 신청 현황 엑셀에 각 지역별 시트를 만들어주는데, 새로운 시트를 파서 수식을 걸어 집계하면 됩니다.

4. def엑셀() 함수의 포항년월 = [], 북구년월 = []를 수정해야합니다. 25년 5월에 한다면 202505로 입력해야 합니다.

5. def동작() 함수의 엑셀()이후에 로그인결제관리와 엑셀다운은 필요에 따라 추가해야 합니다.