# LMS 전산반배정 작업 인수인계

## 2026-06-19 추가인원 14반 퇴장 미실행 원인 및 수정

- 증상: 자동 반배정 후 추가인원이 14반으로 빠지지 않음.
- 원인: `LMS전산반배정.py` 실행부에서 추가인원 입장/퇴장 호출이 주석 처리되어 `학생배정`만 실행되고 있었음.
- 수정:
  - `반배정(page, '망령출동')` 활성화.
  - `반배정(page, '학생배정')` 유지.
  - `반배정(page, '망령퇴장')` 활성화.
- 확인한 엑셀 데이터:
  - `메인` A:C 학생배정 대상 145행.
  - `메인` F:H 추가인원 입장 대상 26행.
  - `메인` K:M 추가인원 퇴장 대상 252행.
  - K:M 퇴장 반은 `BG_MW_14` 128명, `BG_TT_14` 124명으로 모두 14반 대상.
- 검증:
  - `python -m py_compile LMS전산반배정.py` 통과.
  - 실행 호출 순서가 `망령출동 -> 학생배정 -> 망령퇴장`으로 활성화됨.
  - 기존 `#반배정(page, '망령출동')`, `#반배정(page, '망령퇴장')` 주석 호출은 제거됨.
- 주의:
  - 이 기록 시점에는 실제 LMS 반 이동은 실행하지 않았음. 외부 LMS 상태 변경 작업이므로 사용자 명시 지시 후 실행할 것.

## 2026-06-19 추가인원 14반 퇴장 실제 실행

- 사용자 지시로 실제 LMS 퇴장 작업을 실행함.
- 실행 방식:
  - 원본 `LMS전산반배정.py`는 `망령출동 -> 학생배정 -> 망령퇴장` 전체 실행 흐름으로 복구됨.
  - 실제 추가인원 퇴장만 수행하기 위해 `LMS전산반배정_망령퇴장실행.py`를 추가함.
  - 기존 `context.wait_for_event("page")` 타이밍 문제를 피하기 위해 이어실행 스크립트에서는 클릭 전에 `context.expect_page()`를 등록함.
- 실행 결과:
  - 1차 실행: 2행부터 96행까지 `BG_MW_14` 이동 로그 확인 후 97행에서 새 탭 대기 타임아웃 발생.
  - 보정 후 2차 실행: `LMS_GHOST_OUT_START_ROW=97`로 97행부터 253행까지 실행.
  - 254행 아이디 공백에서 `망령퇴장 종료` 메시지 확인.
  - 2차 실행 종료코드 0 확인.
- 검증:
  - `python -m py_compile LMS전산반배정.py LMS전산반배정_망령퇴장실행.py` 통과.
  - `python -m py_compile LMS전산반배정_망령퇴장검증.py` 통과.
  - K:M 퇴장 대상은 252행, 행 범위는 2~253.
  - K:M 고유 아이디는 251개.
  - K:M 중복 아이디는 `blessedew` 1개뿐임.
  - K:M 목표 반 수량은 `BG_MW_14` 128행, `BG_TT_14` 124행.
  - LMS 표본 검증 13건 중 12건 OK.
  - LMS 검증 OK 표본에는 2, 3, 96, 97, 128, 130, 131, 150, 173, 200, 236, 253행이 포함됨.
  - `blessedew` 검색 결과는 LMS에서 화목 `BG_TT_14` 수강 1건만 확인됨.
  - 따라서 129행 `BG_MW_14`와 253행 `BG_TT_14`를 동일 아이디 `blessedew` 하나로 동시에 만족시키는 것은 불가능함.
  - 실제 실행 후 남아 있는 `LMS전산반배정` Python 장기 실행 프로세스 없음.
- 남은 이슈:
  - 129행 `BG_MW_14`에 들어갈 별도 고유 추가인원 아이디가 필요함.
  - 현재 데이터 그대로는 `BG_MW_14`가 127명, `BG_TT_14`가 124명으로 확인됨.
- 추가된 보조 스크립트:
  - `LMS전산반배정_망령퇴장실행.py`
  - `LMS전산반배정_망령퇴장검증.py`
  - `LMS전산반배정_상세페이지확인.py`
  - `LMS전산반배정_아이디검색확인.py`

## 2026-06-19 실행 기록

- 작업 경로: `C:\Users\KJG\Documents\GitHub\enoz_kjg\파이썬\코딩교실\1.코딩교실준비\3.전산반배정\2.LMS전산반배정`
- 대상 파일: `LMS전산반배정.xlsx`, `LMS전산반배정.py`
- 실행 대상 LMS: 북구 `https://enozsw-bukgu.enoz.kr/`
- 강의날짜 값: `202606`
- 지역 prefix: `BG`
- 실행 범위: `학생배정`
- 추가인원 입장/퇴장: 코드에서 주석 처리되어 실행하지 않음

## 사전 점검 결과

- 엑셀 시트 `메인`, `월수`, `화목` 확인.
- `월수` 학생 74명, `화목` 학생 71명, 총 145명 확인.
- 원본 학생 아이디 누락 0건.
- 원본 학생 아이디 중복 0건.
- 생성 대상 반 26개 확인.
- `메인` F:H 추가인원 입장 26명 확인.
- F:H 반 이름 미존재 0건 확인.
- K:M 추가인원 퇴장은 252행이 있으나 이번 실행 대상이 아니므로 미실행.

## 실행 명령 및 결과

1. 최초 실행

```powershell
python 'LMS전산반배정.py'
```

- `LMS전산반배정.xlsx`의 `메인` A:C가 코드 실행 중 자동 생성되어 저장됨.
- 결과: 2행부터 61행까지 완료 로그 확인 후 실패.
- 마지막 성공 로그: 61행 `윤정민(kevin1107)` -> `BG_MW_11`.
- 실패 원인: `context.wait_for_event("page")`가 클릭 이후 실행되어 새 탭 이벤트를 놓친 것으로 보임.
- 실패 메시지: `Timeout 30000ms exceeded while waiting for event "page"`.

2. 이어실행

- 62행부터 이어실행하는 임시 스크립트 `LMS전산반배정_이어실행.py`를 생성해 실행.
- 새 탭 대기를 `context.expect_page()` 방식으로 보정.
- 팝업 페이지가 버튼 클릭 후 자동으로 닫히는 경우를 정상 흐름으로 처리하도록 보정.
- 62행부터 146행까지 완료 로그 확인.
- 종료 로그: `이어실행 종료: 147행 아이디가 비어 있습니다.`
- 이어실행 임시 스크립트는 작업 완료 후 삭제함.

## 사후 검증 결과

- LMS에 다시 로그인하여 샘플 12명을 조회.
- 학생정보의 현재 Group 코드와 선택 목록의 반명을 매칭해 확인.
- 검증 샘플 12/12건 모두 기대 반과 일치.
- 검증 행: 2, 31, 58, 61, 62, 65, 76, 99, 120, 138, 145, 146.
- 확인된 예시:
  - 2행 `김서우(ksw2900)` -> `BG_MW_01`
  - 61행 `윤정민(kevin1107)` -> `BG_MW_11`
  - 62행 `윤준민(jack1107)` -> `BG_MW_11`
  - 146행 `김도현(forjhjj)` -> `BG_TT_13`

## 남은 이슈

- 원본 코드의 새 탭 대기 순서가 불안정함. 다음 실행 전 `page.click()` 전에 `context.expect_page()`를 등록하는 방식으로 코드 개선 권장.
- `Dialog.dismiss: Target page, context or browser has been closed` 경고가 여러 번 출력됐으나 배정 흐름은 계속 진행됨. 팝업/탭 종료 처리 개선 권장.
- `메인` K:M에는 추가인원 퇴장 대상 252행이 남아 있으며, 이번 작업에서는 실행하지 않음. 추후 퇴장 자동화를 켤 경우 별도 검증 필요.

## 2026-06-29 PH LMS 전산반배정 실제 실행

### 요청 요약
- 팀장님 지시로 LMS 전산 반배정 동작을 실제 실행함.
- 대상 파일: `LMS전산반배정.xlsx`, `LMS전산반배정.py`
- 대상 LMS 도메인: `enozsw.enoz.kr`
- 강의날짜: `202606`
- prefix: `PH`

### 실행 전 수정/보정
- 실행 전 원본 백업 생성:
  - `LMS전산반배정.py.bak_20260629-154800`
- 원본 `LMS전산반배정.py` 보정:
  - 새 탭 대기를 `page.click()` 이후 `wait_for_event("page")`에서 `context.expect_page()` 방식으로 변경.
  - 반 선택 실패 시 즉시 `RuntimeError`가 발생하도록 검증 추가.
  - 자동으로 닫힌 상세 탭에서 `close()` 오류가 나지 않도록 방어 처리.
  - 로그에 단계/행번호를 출력하도록 변경.
  - `LMS_GHOST_IN_START_ROW`, `LMS_STUDENT_START_ROW`, `LMS_GHOST_OUT_START_ROW` 환경변수로 이어실행 가능하게 변경.
- 실행/검증 wrapper 추가:
  - `run_lms_assignment.py`
  - `run_lms_assignment_verify.py`

### 사전 점검
- `python -m py_compile` 통과:
  - `LMS전산반배정.py`
  - `run_lms_assignment.py`
  - `run_lms_assignment_verify.py`
- 엑셀 시트 확인: `메인`, `월수`, `화목`
- 자동 생성 학생배정 대상:
  - 총 114명, 고유 ID 114개, 중복 0개
  - 전부 `PH_MW_01`~`PH_MW_15`
- 망령출동 대상:
  - 총 31명, 고유 ID 31개, 중복 0개
  - `PH_MW_01`~`PH_MW_15`, `PH_TT_01`~`PH_TT_16`
- 망령퇴장 대상:
  - 총 300명, 고유 ID 300개, 중복 0개
  - `PH_MW_16` 141명, `PH_TT_17` 159명
- 클래스코드 형식 오류, 반명 공백, ID 공백 없음.

### 실행 결과
- 1차 실행:
  - 로그: `lms_assignment_run_20260629-154955.out.log`
  - `망령출동` 2행 1건 완료 후 Playwright 예외로 중단.
  - stderr: `lms_assignment_run_20260629-154955.err.log` 0 byte
- 이어실행:
  - 환경변수: `LMS_GHOST_IN_START_ROW=3`
  - 로그: `lms_assignment_resume_20260629-155211.all.log`
  - 종료코드: 0
  - 처리 수량:
    - 1차 `망령출동`: 1건
    - 이어실행 `망령출동`: 30건
    - `학생배정`: 114건
    - `망령퇴장`: 300건
  - 합산 처리:
    - `망령출동`: 31/31
    - `학생배정`: 114/114
    - `망령퇴장`: 300/300

### 사후 검증
- 검증 로그: `lms_assignment_verify_20260629-161252.all.log`
- LMS 상세페이지 표본 검증 결과:
  - 학생배정 표본 10/10 OK
  - 망령퇴장 표본 9/9 OK
  - 전체 표본 19/19 OK
- 실행 후 장기 실행 중인 `LMS전산반배정`/Playwright 관련 Python/Chrome 프로세스 없음.
- `LMS전산반배정.xlsx`는 실행 중 `메인` A:C 자동 생성 내용이 저장됨.

### 남은 주의사항
- 실행 로그에는 개인정보가 포함될 수 있으므로 외부 공유 금지.
- `Dialog.dismiss: Target page, context or browser has been closed` 경고가 다수 출력됐으나, 종료코드 0 및 표본 검증 19/19 OK로 실제 배정 결과는 정상 확인됨.
- 다음 실행에서도 새 탭/팝업 관련 경고가 반복될 수 있으나, 현재 보정 코드에서는 처리 흐름이 중단되지 않도록 되어 있음.

## 2026-06-29 PH TT11~TT16 누락 확인 및 현재 상태

- 사용자 보고: `PH_TT_11`~`PH_TT_16` 반이 LMS 반배정 관리/수강 변경 선택지에서 사라짐.
- 원인 확인: `망령출동` 대상 F:H의 `PH_TT_11`~`PH_TT_16` 방지용 계정이 `망령퇴장` 대상 K:M에도 중복 존재했고, 같은 실행에서 `PH_MW_16`으로 다시 이동됨.
- 확인 로그/스크립트:
  - `inspect_lms_class_group_list_20260629-164000.json`: 반배정 관리 목록에서 `PH_TT_11`~`PH_TT_16` 및 `35_11`~`35_16` 그룹이 없음.
  - `inspect_lms_schedule_reg_35_21_20260629-165000.json`: 미스케줄 그룹 `35_21`의 스케줄링 팝업에는 `PH_TT_11`~`PH_TT_16` 강사 선택지는 존재.
  - `verify_lms_tt11_16_20260629-172300.all.log`: 6개 방지용 계정 모두 여전히 `PH_MW_16`으로 조회되어 복구 실패 확인.
- 복구 시도:
  - `run_lms_restore_tt11_16.py`를 수정해 숨은 `hhdTargetGroupNo`와 옵션 재주입 후 `popCourseEditProc.asp` 제출까지 시도.
  - 실행 로그 `lms_restore_tt11_16_20260629-172000.all.log`는 6건 `RESTORE_OK`를 출력했으나, 사후 검증 결과 실제 LMS 상태는 변경되지 않음.
- 현재 결론: `35_11`~`35_16` 그룹 레코드가 LMS 반 목록에 다시 생성되기 전에는 수강 변경만으로 복구 불가.
- 현재 상태: 팀장님께서 반을 수동 재생성 중이며, 생성 완료 보고를 기다리는 상태.
- 후속 조치: 반이 다시 생성되면 `verify_lms_tt11_16.py`로 선택지/현재 배정 확인 후 필요 시 `run_lms_restore_tt11_16.py` 또는 수동 수강변경으로 6개 방지용 계정을 원래 `PH_TT_11`~`PH_TT_16`에 재배정.

## 2026-06-29 PH TT11~TT16 재생성 후 재배정 완료

- 팀장님이 LMS에서 누락 반을 수동 재생성한 뒤, `verify_lms_tt11_16.py`로 사전 확인.
- 사전 확인 로그: `verify_lms_tt11_16_20260629-165900_pre_restore.all.log`
  - `TT_OPTIONS`에 `PH_TT_11`~`PH_TT_16`이 다시 표시됨.
  - 6개 중 5개는 정상, 27행 대상 1건만 `PH_TT_11`이 아니라 `PH_TT_13`에 들어간 상태였음.
- 재배정 실행: `py -3 -X utf8 .\run_lms_restore_tt11_16.py`
- 실행 로그: `lms_restore_tt11_16_20260629-170000_after_manual_class.all.log`
  - 27행 재배정 제출 후 팝업이 닫히며 Playwright가 실패 코드로 종료했으나, 사후 검증 결과 실제 저장은 성공.
- 보정 코드 변경:
  - `run_lms_restore_tt11_16.py`에서 수강 변경 제출 후 팝업이 닫히는 정상 흐름을 실패로 오인하지 않도록 `PlaywrightError`/`PlaywrightTimeoutError` 처리를 추가.
- 최종 검증 로그: `verify_lms_tt11_16_20260629-170500_final.all.log`
  - `PH_TT_11`~`PH_TT_16` 6/6 OK.
  - `TT_OPTIONS`에 `PH_TT_01`~`PH_TT_17` 표시 확인.
  - 관련 Python/Playwright 프로세스 잔류 없음.

## 2026-06-29 PH LMS 화목/추가인원 정리

- 요청: 화목반 전체 반배정 재진행 후, 일반 반에 퍼진 추가인원을 퇴장 반으로 이동.
- 추가 스크립트:
  - `run_lms_assign_tt_students.py`: `화목` 시트 기준 `PH_TT_##` 학생만 선별 배정/검증. `--rows`, `--force-group-no`, `--dump-groups` 옵션 추가.
  - `run_lms_assign_source.py`: `mw`, `tt`, `ghost-in`, `ghost-out-actionable`, `all-safe` 소스별 선별 배정/검증.
  - `summarize_lms_sources.py`: 월수/화목/망령추돌/망령퇴장 소스별 카운트 요약.
- 화목 원천 확인:
  - `lms_tt_assignment_dryrun_20260629-174000.log`
  - 화목 실제 학생 119명, 16개 반, 중복/충돌 0건.
- 화목 배정:
  - `lms_tt_assignment_run_20260629-174100.log`: 119명 중 111명 정상, `PH_TT_10` 8명은 당시 선택지 누락으로 미처리.
  - `PH_TT_10`은 실제 `202606_S2_2_10_1900_35_27`로 확인되어 `lms_tt_assignment_row68_force_group_20260629-180000.log`, `lms_tt_assignment_rows69_75_20260629-180200.log`로 8명 모두 이동 완료.
- 추가인원 퇴장 처리:
  - `lms_ghost_out_actionable_dryrun_20260629-181500.log`: 보호 대상(월수/화목 실제 학생 + 망령추돌 계정)을 제외한 퇴장 대상 269명 산정.
  - 대상 분포: `PH_MW_16` 110명, `PH_TT_17` 159명.
  - 실행 로그: `lms_ghost_out_actionable_run_20260629-181600.log`.
  - 결과: 269/269 OK (`ALREADY_OK` 111, `MOVED_OK` 158).
- 실제 학생 재검증:
  - 화목: `lms_tt_assignment_verify_after_ghostout_20260629-182000.log`에서 119/119 OK.
  - 월수: `lms_mw_full_verify_20260629-182700.log`에서 114/114 OK였으나, 아래 중복 원천 데이터 때문에 최종 상태는 화목 기준을 우선함.
  - 화목 기준 최종 보정: `lms_tt_row53_final_fix_20260629-183100.log`에서 중복 1건을 `PH_TT_07`로 이동.
- 최종 ClassGroupList 요약:
  - 로그: `lms_group_counts_final3_20260629-183100.log`.
  - `PH_TT_11`은 163명 수준에서 8명으로 정리됨.
  - 추가인원 퇴장 반: `PH_TT_17` 159명.
  - 월수 퇴장 반: `PH_MW_16` 134명.
- 남은 이슈:
  - 동일 학생 ID가 월수/화목 원천 시트 양쪽에 1건 존재: `PH_MW_05 / PH_TT_07`.
  - 현재 최종 상태는 사용자의 화목반 재배정 지시를 우선해 해당 1건을 `PH_TT_07`에 둠.
  - 다음 작업자는 이 중복 1건을 엑셀 원천에서 어느 반으로 둘지 확정해야 함.
- 개인정보 주의:
  - 결과 CSV에는 해시 ID만 기록했으며, 보고 시 이름/아이디/전화번호 등 개인정보를 노출하지 말 것.
